# Backend API base URL (no trailing slash).
# IMPORTANT: Use the SAME backend the customer app uses for registration, or the admin
# Users list will show a different database (old users only, no new app signups).
#
# Where users register    → Set this to that backend:
# Local (app on localhost) → VITE_API_BASE_URL=http://localhost:8080
# Production (Railway)     → VITE_API_BASE_URL=https://nurpay-production.up.railway.app
#
# When set, all /api and /api/admin requests go here. Restart dev server after changing.
# VITE_API_BASE_URL=http://localhost:8080
